kind: ResourceQuota
apiVersion: v1
metadata:
  name: {{ .Values.name | quote }}
  namespace: {{ .Values.namespace | quote }}
  labels:
    app: {{ .Values.name | quote }}
spec:
  hard:
    pods: "4"
    requests.cpu: 800m
    requests.memory: 2Gi
    limits.cpu: 1600m
    limits.memory: 4Gi
